<h3><%= @user.name %></h3>
<div class="chat-wrapper">
<%= link_to(public_rooms_path(user_ids: [current_user.id, @profile.user.id]),method: :post) do %>
 <div class="chat-btn">
   <div class="chat-p">ダイレクトメッセージでやり取りする</div>
     <i class="fa-regular fa-message"></i>
   </div>
<% end %>
</div>
<% if @user == current_user %>
<p><%= link_to "プロフィール編集", edit_public_user_path %></p>
<% @posts.each do |post_image| %>
<div>
  <%= link_to public_post_path(post.id) do %>
    <%= image_tag post.get_image.variant(resize: '200x200') %>
  <% end %>
  <p><%= post.title %></p>
</div>
<% end %>
