<div class="search_form">
  <%= form_with url: search_path, local: true, method: :get do |f| %>
    <%= f.text_field :word %>
    <%= f.select :search, options_for_select([["完全一致","perfect_match"], ["部分一致","partial_match"]]) %>
    <%= f.select :dogcat, options_for_select([["犬","犬"], ["猫","猫"]]) %>
    <%= f.submit "検索", class: "btn-sm btn-primary" %>
  <% end %>
</div>

<%= form_with url: public_posts_path, method: :get do |f| %> 
  <%= f.select :coat_id, @coats.map { |co| [co.name, co.id] }, { include_blank: '全ての毛並み' } %> 
  <%= f.submit '検索' %> 
<% end %>

<div class="card-list-container py-5">
<% @posts.each do |post| %>
<div class="card w-50 mx-auto mt-4 shadow-lg">
  <div class="card-body">
    <div class="row">
      <div class="col-10">
        <%= link_to public_post_path(post.id) do %>
        <% post.images.each do |image| %>
          <%= image_tag(image, width:100) %>
        <% end %>
      </div>
        <p class="card-title">タイトル：<%= post.title %></p>
        <p class="card-text">種類：<%= post.kinds %></p>
        <p class="card-text">性別：<%= post.gender %>  年齢：<%= post.age %></p>
        <p class="card-text">募集地域：<%= post.recruitment_area %></p>
      </div>
    </div>
  </div>
</div>
<% end %>
<% end %>